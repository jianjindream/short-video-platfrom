<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="fun.witt.mapper.CollectMapper" >
  <resultMap id="BaseResultMap" type="fun.witt.model.Collect" >
    <id column="id" property="id" jdbcType="BIGINT" />
    <result column="user_id" property="userId" jdbcType="BIGINT" />
    <result column="video_id" property="videoId" jdbcType="BIGINT" />
  </resultMap>

  <insert id="insertIgnore">
    INSERT IGNORE INTO t_collect (user_id, video_id)
    VALUES (#{userId}, #{videoId})
  </insert>

  <delete id="deleteByUserAndVideo">
    DELETE FROM t_collect
    WHERE user_id = #{userId}
      AND video_id = #{videoId}
  </delete>

  <select id="countByUser" resultType="long">
    SELECT COUNT(1)
    FROM t_collect
    WHERE user_id = #{userId}
  </select>

  <select id="countByVideo" resultType="long">
    SELECT COUNT(1)
    FROM t_collect
    WHERE video_id = #{videoId}
  </select>
</mapper>
