<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="fun.witt.mapper.FollowerMapper">
    <resultMap id="BaseResultMap" type="fun.witt.model.Follower">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="from_user_id" property="fromUserId" jdbcType="BIGINT"/>
        <result column="to_user_id" property="toUserId" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <insert id="insertIgnore">
        INSERT IGNORE INTO t_follower (from_user_id, to_user_id, created_at)
        VALUES (#{fromUserId}, #{toUserId}, NOW())
    </insert>

    <delete id="deleteByUsers">
        DELETE FROM t_follower
        WHERE from_user_id = #{fromUserId}
          AND to_user_id = #{toUserId}
    </delete>
</mapper>
